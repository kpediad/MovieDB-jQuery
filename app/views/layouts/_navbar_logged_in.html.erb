<nav class="navbar navbar-expand-lg fixed-top navbar-dark bg-primary">
  <a class="navbar-brand" href="/"><span class="fa fa-film"></span> MovieDB</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarColor01">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <a class="nav-link" href="/movies">Movies <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item dropdown">
        <%= link_to "Account: #{current_user.name}", user_path(current_user), id: "account", class: "nav-link dropdown-toggle", data: {toggle: "dropdown"}, aria: {expanded: "false"} %>
        <div class="dropdown-menu" aria-labelledby="account">
          <%= link_to "Edit Account Details", edit_user_path(current_user), class: "dropdown-item" %>
          <div class="dropdown-divider"></div>
          <%= button_to "Logout", logout_path, method: :delete, data: {confirm: "Are you sure?"}, class: "dropdown-item" %>
        </div>
      </li>
    </ul>
  </div>
</nav>
